spring:
  application:
    name: erudio-config-server
  profiles:
    active:
     - git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/LucasTracker/config-server
          default-label: main
          search-paths:
            - 'greeting-service'
server:
  port: 8888
